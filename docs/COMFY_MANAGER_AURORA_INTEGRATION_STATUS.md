# Aurora + Comfy Manager status

The integration branch keeps Aurora Global as the canonical application and adds a unified capability registry plus a BytePlus Video Agent facade. Existing Aurora ComfyUI Studio and GPU worker infrastructure remains the execution layer.

Full production validation (lint, typecheck, tests, build, real ModelArk request, and live worker smoke test) is still required before production promotion.
