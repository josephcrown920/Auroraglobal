// POST /api/video-agent/stream — standalone exported Video Agent SSE endpoint.
import { createFileRoute } from "@tanstack/react-router";
import { z } from "zod";
import { hasModelArkStreaming, streamModelArkText, wrapModelArkSSE, type ModelArkStreamMessage } from "@/lib/modelark-stream.server";
const Schema=z.object({messages:z.array(z.object({role:z.enum(["system","user","assistant"]),content:z.unknown()})).min(1).max(100),model:z.string().min(1).max(200).optional(),temperature:z.number().min(0).max(2).optional(),maxTokens:z.number().int().min(1).max(32768).optional()});
async function authUserId(req:Request):Promise<string|null>{const{supabaseAdmin}=await import("@/integrations/supabase/client.server");const h=req.headers.get("authorization")||req.headers.get("Authorization");if(!h?.startsWith("Bearer "))return null;const t=h.slice(7);if(t.startsWith("aurk_")){const{userIdForApiKey}=await import("@/lib/cli-device.server");return userIdForApiKey(t)}const{data,error}=await supabaseAdmin.auth.getUser(t);return error||!data.user?null:data.user.id}
export const Route=createFileRoute("/api/video-agent/stream")({server:{handlers:{OPTIONS:async()=>new Response(null,{status:204,headers:{"Access-Control-Allow-Origin":"*","Access-Control-Allow-Methods":"POST, OPTIONS","Access-Control-Allow-Headers":"Content-Type, Authorization"}}),POST:async({request})=>{if(!(await authUserId(request)))return new Response(JSON.stringify({error:"Unauthorized"}),{status:401,headers:{"Content-Type":"application/json","Access-Control-Allow-Origin":"*"}});if(!hasModelArkStreaming())return new Response(JSON.stringify({error:"ModelArk streaming is not configured"}),{status:503,headers:{"Content-Type":"application/json","Access-Control-Allow-Origin":"*"}});try{const input=Schema.parse(await request.json());const upstream=await streamModelArkText({messages:input.messages as ModelArkStreamMessage[],model:input.model,temperature:input.temperature,maxTokens:input.maxTokens,signal:request.signal});return wrapModelArkSSE(upstream,request.signal)}catch(e){return new Response(JSON.stringify({error:e instanceof Error?e.message:String(e)}),{status:500,headers:{"Content-Type":"application/json","Access-Control-Allow-Origin":"*"}})}}}}});
