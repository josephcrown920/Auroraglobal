Here's a paste-ready prompt, tailored to your app (I checked — you have ~70 user-facing pages under `src/routes`, plus the Adult School artifact):

```
Make Aurora visual-first end to end: every user-facing page should communicate
with motion photos, real imagery, and illustrations instead of blocks of text.

SCOPE
Sweep every route in src/routes (landing, home, studio, templates, gallery,
tools, spin, ugc, lipsync, motion, colors, scene-builder, avatar, tiktok,
billing, account, guides, contact, roadmap, empty states, error states, etc.)
plus artifacts/aurora-adult. Treat any section with more than ~2 sentences of
visible copy and no media as a violation to fix.

REPLACEMENT RULES
1. Heroes: replace headline+paragraph blocks with a looping motion photo or
   short video loop of real product output, keeping only a 1-line headline
   and a CTA.
2. Feature lists / bullet points: convert to visual cards — each card shows a
   real rendered image or mini video loop demonstrating the feature, with max
   1 short caption line.
3. How-it-works / instructions: replace numbered text steps with an animated
   visual strip (before→after images, short loops, or illustrated steps).
4. Empty states: replace "nothing here yet" text with an illustration or
   example output image plus one short action line.
5. Stats / testimonials / trust sections: show visual proof (grids of real
   rendered outputs) instead of text claims.
6. Keep text pages (terms, privacy, legal, guides) readable, but give each a
   visual header and illustrated section breaks.

TECHNICAL CONSTRAINTS (do not skip)
- Use the existing Aurora design system utilities and components only; violet
  brand accent; do not invent new styles.
- Every video loop: MP4 with +faststart, muted, autoplay, playsInline, an
  eagerly-loaded poster <img> underneath, and fade the video in on the
  'playing' event (otherwise iOS shows black).
- Respect prefers-reduced-motion: serve the poster/still instead of the loop.
- Images: WebP with srcset where possible; lazy-load everything below the
  fold; do not regress LCP on the landing page.
- Static media goes in public/ and is referenced by plain URL string.
- Accessibility/SEO: keep semantic headings and give every image meaningful
  alt text — replace visible paragraphs, not the page's semantic structure.

DELIVERABLE
Go page by page. For each route, list what text-heavy sections were found and
what visual replaced them. Finish with a checklist of every route confirming
it passes the "no wall of text" rule.
```

One heads-up: a few queued tasks already overlap with this — Task #342 (Add real photos to empty state cards, feature sections, and app inner pages), #343 (interactive photo strip), and #393 (real rendered preview images instead of text cards). If you'd rather I turn this prompt into a proper project task (or split it into a few page-group tasks so they can run in parallel), just say the word.