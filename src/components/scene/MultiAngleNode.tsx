import { Check, Loader2, Play, Rotate3d } from "lucide-react";
export interface AngleNode { id: string; label: string; prompt: string; enabled: boolean; state: "idle" | "running" | "done" | "error"; }
export function MultiAngleNodeBoard({ nodes, running, onToggle, onRunAll, onRunOne }: { nodes: AngleNode[]; running: boolean; onToggle: (id: string) => void; onRunAll: () => void; onRunOne: (id: string) => void }) {
  return <section className="rounded-2xl border border-white/10 bg-black/20 p-4"><div className="mb-3 flex items-center justify-between"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[.18em] text-white/55"><Rotate3d className="size-4 text-primary" /> Multi-angle nodes</div><button onClick={onRunAll} disabled={running} className="rounded-lg bg-white/8 px-2.5 py-1.5 text-[10px] uppercase tracking-widest text-white/70">{running ? <Loader2 className="size-3 animate-spin" /> : <><Play className="mr-1 inline size-3" />Run selected</>}</button></div><div className="grid gap-2 sm:grid-cols-2">{nodes.map((node) => <div key={node.id} className="flex items-center gap-2 rounded-lg border border-white/8 bg-white/[.03] p-2"><input type="checkbox" checked={node.enabled} onChange={() => onToggle(node.id)} className="accent-primary" /><span className="min-w-0 flex-1 text-xs text-white/70">{node.label}</span>{node.state === "done" ? <Check className="size-3 text-emerald-400" /> : node.state === "running" ? <Loader2 className="size-3 animate-spin text-primary" /> : <button onClick={() => onRunOne(node.id)} className="text-[10px] text-white/40 hover:text-white">Run</button>}</div>)}</div></section>;
}